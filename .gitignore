node_modules
dist
*.tsbuildinfo
coverage

# Keep environment variables out of version control
.env
.env.render

/src/generated/prisma

# Local tooling config, not meant for the public repo (project refs, downloaded skills, session artifacts)
.mcp.json
.agents/
.playwright-mcp/
skills-lock.json
